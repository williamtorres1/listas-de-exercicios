#include <stdio.h>
#include <stdlib.h>
int main()
{
	/*
        Faça um programa para, dado o valor dos coeficientes A, B e C,
        calcular e exibir o valor das raízes reais de uma equação do 2º grau  da forma AX2+BX+C=0.
        Lembrete: DELTA = B^2  –  4AC
                  Se DELTA < 0, não existe raiz real.
                  Se DELTA = 0, X1 = X2 = -B / 2*A.

        Se DELTA > 0,
            X1 = - B + DELTA ^(1/2)/ 2 * A
            X2 = - B - DELTA ^(1/2)/ 2 * A
	*/

	system("pause");
	return 1;
}
